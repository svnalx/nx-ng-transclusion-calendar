import { AlexIfDirective } from './alex-if.directive';

describe('AlexIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AlexIfDirective();
    expect(directive).toBeTruthy();
  });
});
